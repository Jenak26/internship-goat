services:
  - type: web
    name: internship-goat-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python listen.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
